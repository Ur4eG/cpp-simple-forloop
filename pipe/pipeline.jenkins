#!groovy

properties([disableConcurrentBuilds()])

pipeline {
  agent {
    label 'master'
  }
  options {
    timestamp()
  }
  stages {
    stage("First step") {
      steps {
        sh 'uname -a'
      }
    }
  }
}
